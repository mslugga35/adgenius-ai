{
  "name": "adgenius-backend",
  "version": "1.0.0",
  "description": "Backend API for AdGenius AI Ad Mockup Generator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "stripe:listen": "stripe listen --forward-to localhost:3001/webhook"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "stripe": "^14.0.0",
    "dotenv": "^16.3.1",
    "@supabase/supabase-js": "^2.39.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}